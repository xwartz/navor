# 行情数据与隐私

Navor 不上传工作区、不收集遥测、不要求账户。`nav serve` 只读取本机选定的工作区。

价格与汇率是可选增强，不是源事实。Reader 会标记缺失、过期或失败的增强，而不会将其当作权威事实。

托管站点上，浏览器向同源的 `POST /api/prices` 请求行情；该代理再把请求的代码转发给配置的数据提供方。代码可能暴露持仓或关注标的。仅在提供方条款、缓存策略与隐私说明适合访客时部署代理。

Navor 整理投资信息，不提供金融、法律、税务或投资建议。
